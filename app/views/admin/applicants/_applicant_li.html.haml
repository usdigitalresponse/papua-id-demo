%li
  .name-and-id
    = link_to applicant.full_name, admin_applicant_url(applicant), class: 'applicant-name'
    %br
    %small.applicant-id= applicant.id
  .applicant-status
    %span.badge{class: "status-#{applicant.status}"}= t("claims.statuses.#{applicant.status}")
